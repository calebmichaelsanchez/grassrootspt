!(function() {
  let productDetail = document.querySelector("#product-detail");

  if (productDetail) {
    // let items = document.querySelectorAll(".also > li");
    // items = [...items];
    // console.log(items)
    // $(items).pick(3);
  }
})();
